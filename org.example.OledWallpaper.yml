app-id: org.example.OledWallpaper
runtime: org.freedesktop.Platform//23.08
sdk: org.freedesktop.Sdk//23.08
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: oled-wallpaper

finish-args:
  - --share=display
  - --share=ipc
  - --socket=wayland
  - --socket=x11
  - --device=dri
  - --filesystem=~/.config/oled-wallpaper:create
  - --env=RUST_LOG=info

modules:
  - name: oled-wallpaper
    buildsystem: simple
    build-commands:
      - . /usr/lib/sdk/rust-stable/enable.sh && cargo build --release
      - install -Dm755 target/release/oled-wallpaper /app/bin/oled-wallpaper
      - install -Dm644 org.example.OledWallpaper.desktop /app/share/applications/org.example.OledWallpaper.desktop
      - install -Dm644 org.example.OledWallpaper.metainfo.xml /app/share/metainfo/org.example.OledWallpaper.metainfo.xml
    sources:
      - type: dir
        path: .
